# Copy to .env (git-ignored) or export in your shell. Never commit real values.
# The variable NAMES are declared in config/default.toml; only the values live here.

# Authenticated SIGAA (discovery, application-state sync, explicit application).
UFAL_SIGAA_USERNAME=
UFAL_SIGAA_PASSWORD=

# SMTP transport for campaign delivery.
PULSAR_SMTP_USER=
PULSAR_SMTP_PASSWORD=
