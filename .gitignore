# Environments and build artifacts
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Secrets
.env
.env.local

# Acquired data, caches and generated artifacts.
# `data/` holds expensive provenance (raw SIGAA/Lattes archives) and fully
# regenerable derived state. Neither belongs in git; back it up separately.
data/

# Never commit patch-installer debris again.
.pulsar-backups/
.pulsar_patch_backups/

# Personal, machine-specific configuration (never shared).
config/local.toml

# Personal academic ledger: the canonical source for `config/profile.yaml`.
# Real identifiers (CPF, RG, telephone) and unpublished manuscript detail.
personal/

# Unpublished manuscripts kept for annexing to outreach. Not ours to publish.
past_works/

# The console's front end is a Vite project. Its dependencies are not vendored;
# its build output (webapp/static) *is* committed, because that is what the
# wheel ships and what `pulsar serve` hands to the browser.
src/pulsar_research/webapp/frontend/node_modules/
